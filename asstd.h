#ifndef ASSTD_H
#define ASSTD_H

#include "asexe.h"

namespace ADscript
{
	void VAR(program* host, char** args);

	void PRINT(program* host, char** args);

	void ADD(program* host, char** args);
}

#endif